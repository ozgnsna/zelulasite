/**
 * List / table–heavy admin screens: wide workspace with calm horizontal padding
 * (not full-bleed; cap scales on very large displays).
 */
export const ADMIN_OPERATIONS_MAIN =
  "mx-auto w-full min-w-0 max-w-[min(100%,92rem)] px-4 sm:px-6 lg:px-8";
